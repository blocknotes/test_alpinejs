{
  "name": "client",
  "version": "1.0.0",
  "main": "index.html",
  "license": "MIT",
  "devDependencies": {
    "@web/dev-server": "^0.1.29"
  },
  "scripts": {
    "start": "web-dev-server --node-resolve",
    "start:debug": "web-dev-server --node-resolve --debug",
    "start:watch": "web-dev-server --node-resolve --debug --watch"
  }
}
